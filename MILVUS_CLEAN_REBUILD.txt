╔════════════════════════════════════════════════════════════════════════════╗
║              Milvus完全清空并重建指南                                       ║
╚════════════════════════════════════════════════════════════════════════════╝

## ✅ 已完成

1. ✓ 修改.env为 `travel_destinations`

---

## 🔧 立即执行删除脚本

```bash
cd backend

python -c "
from pymilvus import connections, utility

print('1. 连接Milvus...')
connections.connect('default', host='localhost', port=19530)

print('2. 列出所有collections...')
collections = utility.list_collections()
print(f'   当前collections: {collections}')

print('3. 删除所有collections...')
for coll in collections:
    print(f'   删除: {coll}')
    utility.drop_collection(coll)

print('4. 确认删除...')
remaining = utility.list_collections()
print(f'   剩余: {remaining}')

if len(remaining) == 0:
    print()
    print('✓✓✓ Milvus已完全清空！')
else:
    print('❌ 还有collection未删除')
"
```

---

## 🚀 完整步骤

### 步骤1: 停止后端
```
Ctrl+C 完全停止
```

### 步骤2: 清空Milvus
```bash
cd backend

python -c "from pymilvus import connections, utility; connections.connect('default', host='localhost', port=19530); cols = utility.list_collections(); [utility.drop_collection(c) for c in cols]; print('✓ 已删除:', cols); print('剩余:', utility.list_collections())"
```

### 步骤3: 重启后端
```bash
python main.py
```

**观察日志应该看到：**
```
[Milvus] Creating hybrid search collection: travel_destinations
[Milvus] Collection created with rich metadata support
[Milvus] Collection loaded: travel_destinations
```

### 步骤4: 上传PDF测试

---

## 📝 预期效果

- Collection名称：`travel_destinations`（原名）
- Schema：12个字段
- 索引：dense_vector + sparse_vector
- 全新干净的collection

══════════════════════════════════════════════════════════════════════════════

立即执行清空命令！

══════════════════════════════════════════════════════════════════════════════
