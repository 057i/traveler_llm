# 🔥 紧急修复指南

## 当前问题
前端显示"AI推荐完成"而不是推荐内容

---

## 🚀 立即执行（3步）

### **1. 强制刷新浏览器**
```
按: Ctrl + Shift + R
或: F12 → 右键刷新按钮 → 清空缓存并硬性重新加载
```

### **2. 打开控制台**
```
按: F12
切换到: Console 标签
```

### **3. 测试查询**
```
输入: "推荐一下三清山的旅游攻略"
观察控制台输出
```

---

## ✅ 应该看到

**控制台日志**:
```
连接SSE事件流: /api/ai-recommend/events/session_xxx
节点开始: {step: "rewrite", ...}
节点完成: {step: "rewrite", ...}
收到AI答案: {answer: "三清山旅游攻略...", sources: [...]}  ← 这个是关键
推荐完成: {message: "AI推荐完成"}
```

**前端显示**:
```
三清山旅游攻略推荐

玉京峰景区是三清山的最高峰...
女神峰是三清山最具代表性的景观...
三清山作为中国道教名山...

📚 参考来源 (3条)
```

---

## ❌ 如果还是不行

### **问题A: 控制台没有"收到AI答案"**

**原因**: 前端代码没更新

**解决**:
1. 完全关闭浏览器
2. 重新打开新标签页
3. 访问 http://localhost:5173
4. Ctrl+Shift+R 强制刷新

### **问题B: 有"收到AI答案"但没显示**

**原因**: 数据格式问题

**检查**: 
- 控制台点开"收到AI答案"查看data
- 确认有`answer`字段
- 确认有`sources`数组

---

## 🔍 调试命令

如果上述方法都不行，在浏览器控制台执行：

```javascript
// 检查是否有result监听器
console.log(eventSource)
console.log(eventSource.listeners)

// 手动触发测试
messages.value.push({
  type: 'assistant',
  message: '这是测试消息，如果能看到说明渲染正常',
  sources: [],
  nodes: []
})
```

---

## 📞 提供信息

如果问题仍然存在，请提供：

1. **浏览器控制台完整日志** (Console标签)
2. **Network标签的events请求** (查看是否有result事件)
3. **前端显示的截图**

---

**现在立即执行步骤1-3！** 🚀
