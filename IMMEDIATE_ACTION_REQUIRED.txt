╔════════════════════════════════════════════════════════════════════════════╗
║              立即执行 - Milvus Collection删除和日志添加                     ║
╚════════════════════════════════════════════════════════════════════════════╝

## ⚠️ 当前问题确认

### 问题1: Milvus Schema不匹配
**错误：** `expect 13 list, got 12`
**原因：** 旧collection有13个字段，新代码12个字段
**状态：** 提取了42个景点，但插入失败

### 问题2: 需要查看实体的省市信息
**需求：** 打印每个提取实体的省份、城市

---

## 🚀 立即执行步骤

### 步骤1: 删除旧Milvus Collection
```bash
cd backend

python -c "from pymilvus import connections, utility; connections.connect('default', host='localhost', port=19530); utility.drop_collection('travel_destinations'); print('✓ 已删除旧collection')"
```

**预期输出：**
```
✓ 已删除旧collection
```

---

### 步骤2: 添加实体提取日志

**修改文件：** `backend/app/workflows/document_processing/nodes/node_entity_extraction.py`

**在第110行左右添加日志：**

找到这段代码：
```python
if isinstance(dest, dict) and dest.get('name'):
    # Ensure all required fields exist
    destination: Destination = {
        'name': dest.get('name', ''),
        'city': dest.get('city', ''),
        'province': dest.get('province', ''),
        ...
```

在创建destination之后，添加：
```python
# 打印每个实体的省市信息
logger.info(
    f"[Extract] 实体: {dest.get('name', 'Unknown')} | "
    f"省份: {dest.get('province', '未识别')} | "
    f"城市: {dest.get('city', '未识别')} | "
    f"类型: {dest.get('type', 'Unknown')}"
)
```

**完整修改示例：**
```python
if isinstance(dest, dict) and dest.get('name'):
    # 打印实体信息
    logger.info(
        f"[Extract] 实体: {dest.get('name', 'Unknown')} | "
        f"省份: {dest.get('province', '未识别')} | "
        f"城市: {dest.get('city', '未识别')} | "
        f"类型: {dest.get('type', 'Unknown')}"
    )

    # Ensure all required fields exist
    destination: Destination = {
        'name': dest.get('name', ''),
        'city': dest.get('city', ''),
        'province': dest.get('province', ''),
        ...
    }
    all_destinations.append(destination)
```

---

### 步骤3: 重启后端
```bash
python main.py
```

**观察启动日志：**
```
[Milvus] Creating hybrid search collection with rich metadata
[Milvus] Collection created with rich metadata support
```

---

### 步骤4: 重新上传PDF

上传PDF后，观察日志会看到：
```
[Extract] 实体: 三清山 | 省份: 江西省 | 城市: 上饶市 | 类型: Attraction
[Extract] 实体: 庐山 | 省份: 江西省 | 城市: 九江市 | 类型: Attraction
...
[Extract] Total extracted: 42 destinations
[Vector] Generating dense vectors for 42 destinations
[Vector] Generating sparse vectors for 42 destinations
[Milvus] Inserted 42 entries with complete metadata
[Vector] Hybrid vectorization complete: 42 destinations
```

---

## ✅ 预期结果

### 统计数据会显示：
```
总文档: 2
省份数: X (从Neo4j查询)
城市数: X (从Neo4j查询)
总景点数: 42+88=130
文本RAG: 130条
图RAG: 130节点
```

### 文档列表会显示：
```
文档名称  省份  城市  提取景点数  页数  状态
xxx.pdf   -     -     42         -     完成
```

---

## 🎯 完成清单

- [ ] 删除旧Milvus collection
- [ ] 添加实体提取日志
- [ ] 重启后端
- [ ] 重新上传PDF
- [ ] 验证数据正确显示

══════════════════════════════════════════════════════════════════════════════

现在立即执行步骤1删除旧collection！

══════════════════════════════════════════════════════════════════════════════
